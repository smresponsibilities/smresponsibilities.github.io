# 32: Remove rejected assets and restart from verified references

Status: resolved
Type: research
Priority: highest
Supersedes: 31

## User direction

Delete all generated or derived casing assets and restart from scratch. Gather ten distinct
references for each generation. The rejected prototypes have incorrect outer faces, moving
button boundaries, and active gaps between buttons. Do not reuse them as a new implementation.

## Scope

- Generations I through IX, ten distinct references each. Remakes and alternate continuities
  must be labelled and must not be merged into one fabricated device.
- Remove generated masters, crops, assembly previews, and prototype screenshots. Preserve
  downloaded source references, production code, user files, and tickets 20 and 21.
- Keep a recoverable file manifest, outside-project recovery location, and content hashes.
- Retire the rejected demo routes. Replace their visible content with a link to the new study.
- Record hardware geometry evidence separately from game-screen and input evidence. Ten UI
  images are not proof of ten outer-case angles.
- Write a new mechanical and button acceptance checklist before creating replacement art.

## Acceptance

- [x] Generated and derived assets removed from active project and this task's generation cache.
- [x] Preserved reference hashes are unchanged.
- [x] Ten verified, distinct references for every generation I through IX.
- [x] Sources record variant, provenance, visible evidence, and limits.
- [x] Fresh reference catalogue can be opened without the rejected casing assets.
- [x] New checks cover inert gaps, fixed housing, held face travel, release/cancel, matching
      inner/outer geometry, and real UI results. No new art is claimed to pass them yet.

## Evidence

Removal manifest: `../research/restart/removed-assets-manifest.json`.
New reference notes: `../research/restart/`.

## Answer

Removed 202 generated or derived files recoverably, retaining the 42 existing reference files
and eight downloaded source masters unchanged. Six rejected demo routes now show removal notices.
The new catalogue has 90 downloaded, hash-verified reference items, ten per generation I through
IX. Hardware, software, concept, and alternate-anime evidence are labelled separately. Ten items
do not imply ten canonical hardware views. See `../research/restart/REFERENCES.md` and
`../research/restart/VERIFICATION.md`.

The user chose a hybrid device: image casing and matched lid faces, coded source-matched caps,
fixed wells and gaps, and live portfolio screens. Cross-checking the old prompt found conflicting
D-pad splitting, well-clearance, and Kanto provenance instructions. The historical prompt now
points to `../research/restart/REBUILD-PROMPT.md`. No replacement imagery or interaction is built
or approved by this research ticket.

## Handoff

**Built:** Recoverable cleanup, retired demo entry points, a browser-readable 90-reference board,
source notes, corrected hybrid build prompt, and mechanical/button acceptance checks.

**Deviated:** The old raster-only, split-D-pad prompt is retired in favour of the user's latest
hybrid requirement. References include software and alternate continuity; hardware-only coverage
is not claimed.

**Watch out:** No old visual approval carries forward. Do not restore old art from Git or the
recovery directory into the new design. Recovery is only for undoing deletion if requested.
Kanto's original RG reference shows the open interior only. It cannot prove a closed exterior.
The clean Gen-I redraw used earlier has unverified original-art provenance. Do not silently
combine game and anime geometry. The next build needs an explicit baseline and missing-face plan.
