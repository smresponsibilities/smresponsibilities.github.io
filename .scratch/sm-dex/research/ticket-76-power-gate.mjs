import {chromium} from 'playwright-core';
const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage({viewport:{width:1280,height:900}});const results={};
for(const id of ['red','x','sword-shield','scarlet-violet']){await page.goto(`http://127.0.0.1:4321/?gen=${id}`,{waitUntil:'load'});await page.waitForSelector('#page-loader',{state:'hidden'});results[id]=await page.evaluate(()=>({outerHidden:document.querySelector('#power').hidden,readerHidden:document.querySelector('#reader-controls [data-action="power"]')?.hidden,powered:!document.querySelector('.glass')?.classList.contains('off')}));}
const checks={legacyHasPower:!results.red.outerHidden&&!results.red.readerHidden,modernNoPower:['x','sword-shield','scarlet-violet'].every(id=>results[id].outerHidden&&results[id].readerHidden&&results[id].powered)};console.log(JSON.stringify({results,checks},null,2));await browser.close();if(Object.values(checks).some(v=>!v))process.exit(1);
